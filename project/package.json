{
  "scripts": {
    "start": "servor --reload",
    "dev": "servor --reload",
    "build": "echo 'No build step required - static files'",
    "preview": "servor --no-reload",
    "test": "start http://localhost:8080 && servor --reload",
    "deploy": "wrangler pages publish ."
  },
  "dependencies": {
    "servor": "^4.0.2",
    "prismjs": "^1.29.0"
  }
}
