{
  "name": "html-tutorial",
  "version": "1.0.0",
  "description": "Ein Tutorial für HTML, CSS und JavaScript",
  "main": "index.js",
  "scripts": {
    "pages:dev": "wrangler pages dev ./project",
    "pages:deploy": "wrangler pages deploy ./project",
    "dev": "live-server ./project --host=localhost --port=3000 --entry-file=index.html --mount=/:/project",
    "start": "npm run dev --port=3000"
  },
  "keywords": [
    "html",
    "css",
    "javascript",
    "tutorial"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "wrangler": "^3.0.0"
  }
}
